
To use the HEA_criteria.py module, use 'from HEA_criteria import *'.
Given a chemical composition of the alloy, it is possible to determine:
  - the atomic fraction : atomic_percentage(<alloy>),
  - mixing enthalpy: dHmix(<alloy>), 
  - difference in atomic radius : atomic_radii(<alloy>),
  - Pauling electronegativity: electronegativity(<alloy>),
  - VEC : vec(<alloy>),
  - mixing entropy: dSmix(<alloy>),
  - elastic deformation energy: dHel(<alloy>).
    - The atomic volume is calculated as: (4/3) * pi() * atomic_radii^3 * 6.02 * 10^23 * 10^-30 [m³]
Files 'element_radii_electro_vec.csv', 'Miedema_paired_dHmix.csv', and 'physical_values.csv' contain values taken from the articles
stored in this folder.
